from character import Character
from kokichi import Kokichi
from base import min_height, max_height, width

kokichi = Kokichi(pos=(20, 240), rangec=[0, min_height, width - 50, max_height])
kaito = Character(pos=(600, 230), image="resources/pics/luminary_of_the_stars.png", name="Kaito", color=(149, 38, 171))
